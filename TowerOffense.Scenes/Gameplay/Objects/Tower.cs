using Microsoft.Xna.Framework;
using TowerOffense.Objects.Common;

namespace TowerOffense.Scenes.Gameplay.Objects {
    public class Tower : Entity {
        public Tower(Scene scene, Point position, Point size, int titleBarHeight = 24, int borderThickness = 1) : base(scene, position, size, titleBarHeight, borderThickness) {
            // STUB
        }
    }
}